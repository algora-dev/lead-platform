'use client';import {useEffect,useState} from 'react';
type Run={id:number;country:string;startedAt:string;status:string;deepOffset:number;searchRequests:number;resultsFound:number;pagesFetched:number;duplicateAdverts:number;advertsSaved:number;companiesCreated:number;companiesUpdated:number;contactsFound:number;errors:number;message?:string};
export default function SourceWorkspace(){const [runs,setRuns]=useState<Run[]>([]),[running,setRunning]=useState(''),[notice,setNotice]=useState('');const load=()=>fetch('/api/scans').then(r=>r.json()).then(setRuns);useEffect(()=>{load()},[]);const scan=async(country:'UK'|'NZ')=>{setRunning(country);setNotice(`Scanning ${country}…`);const r=await fetch('/api/scans',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({country})});const d=await r.json();setNotice(r.ok?d.output:d.error);setRunning('');load()};return <><div className="grid2" style={{marginBottom:16}}><div className="card"><h2 style={{marginTop:0}}>Brave Search</h2><p className="muted">Each scan revisits fresh results and rotates through deeper pages. New adverts strengthen existing company profiles instead of creating duplicate leads.</p><div className="toolbar"><button disabled={!!running} onClick={()=>scan('UK')}>{running==='UK'?'Scanning UK…':'Scan UK'}</button><button disabled={!!running} onClick={()=>scan('NZ')}>{running==='NZ'?'Scanning NZ…':'Scan NZ'}</button></div><div className="muted">{notice||'Your existing BRAVE_API_KEY in .env is used.'}</div></div><div className="card"><h2 style={{marginTop:0}}>Company scoring</h2><p><strong>Only positive evidence adds points.</strong></p><p className="muted">Operational task signals, multiple adverts, repeated tasks, salary investment, contact details and the 10–150 employee sweet spot all increase priority. No industry or job type is penalised.</p></div></div><div className="card"><h2 style={{marginTop:0}}>Recent scans</h2><div style={{overflow:'auto'}}><table><thead><tr><th>Started</th><th>Country</th><th>Page</th><th>Requests</th><th>Results</th><th>Fetched</th><th>Duplicates</th><th>Adverts</th><th>New companies</th><th>Updated</th><th>Contacts</th><th>Errors</th></tr></thead><tbody>{runs.map(r=><tr key={r.id}><td>{new Date(r.startedAt).toLocaleString()}</td><td>{r.country}</td><td>0 + {r.deepOffset}</td><td>{r.searchRequests}</td><td>{r.resultsFound}</td><td>{r.pagesFetched}</td><td>{r.duplicateAdverts}</td><td>{r.advertsSaved}</td><td>{r.companiesCreated}</td><td>{r.companiesUpdated}</td><td>{r.contactsFound}</td><td>{r.errors}</td></tr>)}</tbody></table>{!runs.length&&<div className="empty">No scans yet.</div>}</div></div></>}
